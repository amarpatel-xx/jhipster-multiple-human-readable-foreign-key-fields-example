apply plugin: "war"

bootWar {
    mainClass = "com.okta.developer.store.OrchestratorApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
