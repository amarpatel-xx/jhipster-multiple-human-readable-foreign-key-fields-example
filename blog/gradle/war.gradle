apply plugin: "war"

bootWar {
    mainClass = "com.saathratri.developer.blog.BlogApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
